# Payment secret key (server-side only — never expose as NEXT_PUBLIC_*)
KORAPAY_SECRET_KEY=

# Public site URL - required for checkout redirects and share previews
# Production example: https://sectioneight.ng
NEXT_PUBLIC_APP_URL=http://localhost:3000
