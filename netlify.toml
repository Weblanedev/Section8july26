[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Fix "Page not found" on direct URL / refresh for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
